# Core dependencies
nltk==3.9.4
icecream==2.1.10
python-dotenv==1.2.2
openai==2.30.0
numpy==2.2.6
tiktoken==0.12.0
langchain-text-splitters==1.1.1
pydantic==2.12.5
beautifulsoup4==4.14.3
scikit-learn==1.7.2
lxml==4.8.0
more-itertools==8.10.0
joblib==1.5.3
EbookLib==0.20
Pillow==12.3.0
pypandoc==1.17

# spaCy for NER (CPU version - stable)
# For GPU support, install: pip install "spacy[cuda12x]" instead
spacy==3.8.13
# spacy-transformers  # Optional: only for transformer-based NER models

# PyTorch (CPU version - stable)
# For GPU support with CUDA 12.1, install:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch==2.11.0
# torchvision
# torchaudio

# CuPy for GPU acceleration (optional)
# Only needed if using GPU for NER/cosine similarity
# May cause NVRTC errors on some CUDA 12 setups
# If you get "nvrtc: error: invalid value for --gpu-architecture", remove cupy and use CPU
# cupy-cuda12x

# Thinc (required by spaCy)
thinc==8.3.13

# Encoding detection for TXT files
charset-normalizer==3.4.6
chardet==4.0.0
